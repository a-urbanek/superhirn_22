# Konfigurationen
ROWS = 10  # Eine zusätzliche Reihe für die Farben
COLUMNS = 5
CELL_SIZE = 50
GAP_SIZE = 5

CELL_COLOR = (169, 169, 169)
BORDER_COLOR = (0, 0, 0)
BORDER_WIDTH = 2
FEEDBACK_COLUMNS = 5
FEEDBACK_ROWS = 10
FEEDBACK_CELL_SIZE = 30
FEEDBACK_GAP_SIZE = GAP_SIZE
COLORS = [(255, 0, 0), (0, 255, 0), (0, 0, 255), (255, 255, 0), (255, 0, 255), (0, 255, 255)]
FPS = 60
MARGIN = 30
WIDTH = (COLUMNS * (CELL_SIZE + FEEDBACK_CELL_SIZE) + (COLUMNS - 1) * (FEEDBACK_GAP_SIZE + GAP_SIZE) + 3 * MARGIN) if (COLUMNS * (CELL_SIZE + FEEDBACK_CELL_SIZE) + (COLUMNS - 1) * (FEEDBACK_GAP_SIZE + GAP_SIZE) + 3 * MARGIN) > 0 else 0
HEIGHT = (ROWS + 1) * (CELL_SIZE + GAP_SIZE) + 3 * MARGIN

button_width = 100  # Breite des Buttons
button_height = 30  # Höhe des Buttons
